# Set PUBLIC_ADDR to a fixed address to override deterministic machine-id
# generation. The default is intentionally a locally administered unicast
# address because the Q8B device tree does not contain a public BT address.
PUBLIC_ADDR=auto

